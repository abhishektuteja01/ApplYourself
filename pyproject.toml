[project]
name = "applyourself"
version = "0.1.0"
description = "Human-gated, config-driven job-search pipeline: scrape, score, tailor."
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "abhishektuteja01" }]
requires-python = ">=3.12,<3.13"
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: End Users/Desktop",
    "Natural Language :: English",
    "Operating System :: MacOS",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3.12",
    "Topic :: Office/Business",
    "Topic :: Text Processing :: Markup",
    "Private :: Do Not Upload",
]
dependencies = [
    # Upper bound: jobspy_source calls LinkedIn._get_job_details, a private API.
    "python-jobspy>=1.1.80,<1.2",
    "pandas>=2.2",
    "pyarrow>=15.0",  # parquet engine for pandas — no direct imports
    "rapidfuzz>=3.6",
    "python-docx>=1.1",
    "lxml>=5.0",  # direct XML surgery in scripts/scrub_example_templates.py
    "pyyaml>=6.0",
    "requests>=2.32",
    "geonamescache>=3.0.1",
]

[project.urls]
Homepage = "https://github.com/abhishektuteja01/ApplYourself"
Repository = "https://github.com/abhishektuteja01/ApplYourself"
Issues = "https://github.com/abhishektuteja01/ApplYourself/issues"

[project.scripts]
ingest-url = "src.discovery.ingest_url:main"
score = "src.score_cli:main"
track = "src.track_cli:main"
tailor-prep = "src.tailor_cli:main"
profile-extract = "src.profile_extract:main"
discover = "src.discovery.orchestrator:main"
verticals-check = "src.verticals:main"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-p no:cacheprovider -ra --strict-markers"
filterwarnings = ["error"]
